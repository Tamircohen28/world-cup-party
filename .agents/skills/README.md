# Portable agent skills

Tool-agnostic skills for this repo live here so Claude Code, Cursor, and Codex can share them.
Each skill is a subdirectory with a `SKILL.md`. None are defined yet — add them as repeatable
agent workflows emerge (e.g. "add a fixture", "wire a new Base44 entity").

Canonical agent guidance: [AGENTS.md](../../AGENTS.md).
